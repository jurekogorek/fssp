<% content_for :head do %>
    <%= javascript_include_tag  "jquery.ui.datepicker-pl.js"%>
    <%= javascript_include_tag "jquery.dd.js" %>
<script type="text/javascript">
	$(function() {  
	  try {
	    $("#powiadomienie_starts_at").datepicker($.datepicker.regional['pl']);
	    $("#powiadomienie_ends_at").datepicker($.datepicker.regional['pl']);
	  } catch(e) {
	    alert(e.message);
	  }
	});
</script>
<%end%>

<%= form_for(@powiadomienie,html: {class: "form-horizontal"}) do |f| %>
  <% if @powiadomienie.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@powiadomienie.errors.count, "error") %> prohibited this plik from being saved:</h2>

      <ul>
      <% @powiadomienie.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	<div class="control-group">
		<%= f.label :message ,class: "control-label"%>
		<div class="controls">
			<%= f.text_area :message, class: "input-xlarge" %>
		</div>
	</div>
	<div class="control-group">
		<%= f.label :confirm ,class: "control-label"%>
		<div class="controls">
			<%= f.text_field :confirm %>
		</div>
	</div>
	<%= tinymce_assets %>
	<div class="control-group">
		<%= f.label :more ,class: "control-label"%>
		<div class="controls">
			<%= f.text_area :more, :class => "tinymce",:rows=>20,:cols=>70%>
			<%= tinymce %>
		</div>
	</div>
	<div class="control-group">
		<%= f.label :more_confirm ,class: "control-label"%>
		<div class="controls">
			<%= f.text_field :more_confirm %>
		</div>
	</div>	
	<div class="control-group">
		<%= f.label :starts_at ,class: "control-label"%>
		<div class="controls">
			<%= f.text_field :starts_at %>
		</div>
	</div>
	<div class="control-group">
		<%= f.label :ends_at ,class: "control-label"%>
		<div class="controls">
			<%= f.text_field :ends_at %>
		</div>
	</div>	
	
  <div class="form-actions">
    <%= f.submit class: "btn btn-danger"%>
  </div>
<% end %>

<script type="text/javascript">

	$(function() {  
	  try {
	    $("#powiadomienie_starts_at").datepicker($.datepicker.regional['pl']);
	    $("#powiadomienie_ends_at").datepicker($.datepicker.regional['pl']);
	  } catch(e) {
	    alert(e.message);
	  }
	});
</script>